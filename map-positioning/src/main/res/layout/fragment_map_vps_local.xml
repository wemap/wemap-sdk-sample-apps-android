<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/gray"
    tools:context="fragments.MapVPSLocalFragment">

    <FrameLayout
        android:id="@+id/mapLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.getwemap.sdk.map.WemapMapView
            android:id="@+id/mapView"
            android:layout_width="match_parent"
            android:layout_height="match_parent" />

        <com.getwemap.example.common.map.MapLevelsSwitcher
            android:id="@+id/levelsSwitcher"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_vertical|end"
            android:layout_margin="16dp"
            android:visibility="invisible"
            app:selectionRequired="true"
            app:singleSelection="true"
            tools:visibility="visible" />

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:orientation="vertical">

            <FrameLayout
                android:id="@+id/containers"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                app:layout_constraintBottom_toBottomOf="parent">

                <LinearLayout
                    android:id="@+id/poi_container"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="#DDDDDDDD"
                    android:orientation="vertical"
                    android:padding="16dp"
                    android:visibility="gone">

                    <TextView
                        android:id="@+id/poi_info"
                        style="@style/TextAppearance.AppCompat.Title"
                        android:layout_width="match_parent"
                        android:layout_height="wrap_content"
                        android:gravity="center_horizontal" />

                </LinearLayout>

            </FrameLayout>

            <ImageButton
                android:id="@+id/camera"
                android:layout_width="56dp"
                android:layout_height="56dp"
                android:layout_marginEnd="16dp"
                android:layout_marginBottom="8dp"
                android:background="@drawable/bg_circle"
                android:contentDescription="@string/camera"
                android:scaleType="center"
                android:src="@drawable/photo_camera_24px"
                android:visibility="gone"
                app:layout_constraintBottom_toTopOf="@id/locate_me"
                app:layout_constraintEnd_toEndOf="parent"
                app:tint="@color/black" />

            <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:id="@+id/locate_me"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_margin="16dp"
                android:contentDescription="@string/locate_me"
                android:src="@drawable/location_searching_24px"
                app:backgroundTint="@color/white"
                app:backgroundTintMode="add"
                app:layout_constraintBottom_toTopOf="@id/containers"
                app:layout_constraintEnd_toEndOf="parent" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- Compact status chip surfaced over the map during continuous background scanning,
             when the full-screen scan overlay (and its scan_status_text) is hidden. The dot is the
             liveness indicator: it flashes on every scan event and its colour tells whether the scan
             loop is running, merely quiet (movement-aware cadence), or stalled. -->
        <LinearLayout
            android:id="@+id/map_scan_status_chip"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="top|center_horizontal"
            android:layout_margin="16dp"
            android:background="#DD222222"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingHorizontal="16dp"
            android:paddingVertical="8dp"
            android:visibility="gone"
            tools:visibility="visible">

            <View
                android:id="@+id/scan_activity_dot"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:layout_marginEnd="8dp"
                android:background="@drawable/bg_circle"
                android:backgroundTint="@color/scan_active" />

            <TextView
                android:id="@+id/map_scan_status_text"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:gravity="center"
                android:textColor="@color/white"
                tools:text="Scanning • 3 fixes • 2s ago\nLocalized ✓" />

        </LinearLayout>

    </FrameLayout>

    <FrameLayout
        android:id="@+id/cameraLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:visibility="invisible"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <androidx.camera.view.PreviewView
            android:id="@+id/previewView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="top" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="top"
            android:layout_margin="16dp"
            android:orientation="vertical">

            <TextView
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                style="@style/TextAppearance.AppCompat.Title"
                android:background="#DDDDDD"
                android:gravity="center"
                android:padding="16dp"
                android:text="Scan your surroundings by pointing camera to shops or signs around you." />

            <!-- Live scan status: updated on every localization, scan outcome, and error so the
                 scanner's behaviour is visible during on-device tests (not just in logcat). -->
            <TextView
                android:id="@+id/scan_status_text"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginTop="8dp"
                android:background="#DD222222"
                android:gravity="center"
                android:padding="8dp"
                android:textColor="@color/white"
                android:visibility="gone"
                tools:text="Scanning…"
                tools:visibility="visible" />

        </LinearLayout>

        <androidx.appcompat.widget.SwitchCompat
            android:id="@+id/continuous_scan_switch"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_marginBottom="70dp"
            android:background="#DDDDDD"
            android:checked="true"
            android:padding="8dp"
            android:text="Keep scanning to auto-correct (continuous)" />

        <Button
            android:id="@+id/stop_scan_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center_horizontal|bottom"
            android:layout_marginBottom="20dp"
            android:text="Cancel" />

        <TextView
            android:id="@+id/camera_debug_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="16dp"
            android:layout_marginBottom="140dp"
            android:background="#DDDDDDDD"
            android:visibility="invisible" />

    </FrameLayout>

</androidx.constraintlayout.widget.ConstraintLayout>
